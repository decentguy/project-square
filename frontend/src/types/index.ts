export interface CaseStudy {
    title: string;
    description: string;
}

export interface Brand {
    id: string;
    name: string;
    logo: string;
}

export interface DisplayDTO {
    title: string,
    image: string,
    description: string  
}
